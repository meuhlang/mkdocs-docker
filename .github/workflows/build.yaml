name: Build

on: push

jobs:
  build:
    name: Build
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2.3.5
      - name: Build image
        run: docker build --tag "local/${GITHUB_REPOSITORY}" ./src
